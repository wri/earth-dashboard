import NewsTopicLayoutComponent from "./component";
import { connect } from "react-redux";
import { setIsMobile } from "slices/common";

// Temporary bridging design
const NewsIndex = ()) => {
    return <div>

    </div>
}

export default connect(state => ({}), {
  setIsMobile
})(NewsTopicLayoutComponent);
