import showMapGrid from "schemas/global-settings/show-map-grid";
import animations from "schemas/global-settings/animations";
import highDefinitionMode from "schemas/global-settings/high-definition-mode";
import basemaps from "schemas/global-settings/basemaps";

const globalSettingsSchema = [showMapGrid, animations, highDefinitionMode, basemaps];

export default globalSettingsSchema;
